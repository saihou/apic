<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/uberActivityBackground"
    tools:context="com.space.apic.UberTripExperience">

        <include
            android:id="@+id/toolbar"
            layout="@layout/uber_toolbar">
        </include>

         <fragment android:name="com.space.apic.UberVideoFragment"
             android:id="@+id/video_fragment"
             android:layout_below="@id/toolbar"
             android:layout_width="match_parent"
             android:layout_height="wrap_content"/>

        <fragment android:name="com.space.apic.RestaurantMenuFragment"
            android:id="@+id/menu_fragment"
            android:layout_below="@id/video_fragment"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

</RelativeLayout>
